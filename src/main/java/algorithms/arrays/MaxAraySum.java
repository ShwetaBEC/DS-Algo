package algorithms.arrays;

public class MaxAraySum {

	public static void main(String[] args) {

		/*
		 * There are n houses build in a line, each of which contains some value in it.
A thief is going to steal the maximal value of these houses,
but he can’t steal in two adjacent houses because
the owner of the stolen houses will tell his two neighbors left and right side.
 What is the maximum stolen value?
		 */
		
		/**
		 * Give a tree, give the maximum sum having from root to leaf
		 */
		
		/**
		 * Given a sequence of 0s and 1s find the maximum length subsequence of continuous 1s.
           And return the starting and end position.
  
           If any one 0 is changed to 1, then give the maximum array having 1s
		 */
		
		
	}

}
